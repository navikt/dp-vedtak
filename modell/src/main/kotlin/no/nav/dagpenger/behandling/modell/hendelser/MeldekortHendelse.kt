package no.nav.dagpenger.behandling.modell.hendelser

class MeldekortHendelse
/*class MeldekortHendelse(
    meldingsreferanseId: UUID,
    ident: String,
    meldekortId: UUID,
    meldekortFraOgmed: LocalDate,
    meldekortTilOgmed: LocalDate,
    dager: Map<LocalDate, Int>,
    opprettet: LocalDateTime,
) : StartHendelse(meldingsreferanseId, ident, SøknadId(søknadId), gjelderDato, fagsakId, opprettet) {
    override fun regelkjøring(opplysninger: Opplysninger): Regelkjøring = Regelkjøring(skjedde, opplysninger, Søknadsprosess())

    override fun avklarer(opplysninger: LesbarOpplysninger) = KravPåDagpenger.kravPåDagpenger

    override fun behandling() =
        Behandling(
            this,
            listOf(),
        )

    override fun kontrollpunkter() = emptyList<Kontrollpunkt>()
}
*/
